import { Command } from 'clipanion';
/**
 * This command allows to evaluate a deployment gate in Datadog.
 * It handles the entire process of requesting a gate evaluation and polling for results
 * The command will exit with status 0 when the gate passes and status 1 otherwise.
 */
export declare class DeploymentGateCommand extends Command {
    static paths: string[][];
    static usage: import("clipanion").Usage;
    private service;
    private env;
    private identifier;
    private version;
    private apmPrimaryTag;
    private timeout;
    private failOnError;
    private monitorVariable;
    private fips;
    private fipsIgnoreError;
    private config;
    private logger;
    private evaluationRequestTimeout;
    private pollingInterval;
    private startTime;
    execute(): Promise<0 | 1>;
    private getApiHelper;
    private buildEvaluationRequest;
    private requestGateEvaluation;
    private pollForEvaluationResults;
    private getEvaluationResult;
    private getResultForDatadogError;
    private getRetryOptions;
}

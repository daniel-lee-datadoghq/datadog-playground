import { Command } from 'clipanion';
export declare const parseMeasures: (measures: string[]) => {};
export declare class MeasureCommand extends Command {
    static paths: string[][];
    static usage: import("clipanion").Usage;
    private level;
    private measures;
    private measuresFile;
    private noFail;
    private fips;
    private fipsIgnoreError;
    private config;
    execute(): Promise<number>;
    private sendMeasures;
    private handleError;
}

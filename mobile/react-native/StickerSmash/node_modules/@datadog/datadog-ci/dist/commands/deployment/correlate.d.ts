import { Command } from 'clipanion';
/**
 * This command collects environment variables and git information to correlate commits from the
 * source code repository to the configuration repository. This allows to connect pipelines triggering
 * changes on the configuration repository to deployments from gitOps CD providers
 */
export declare class DeploymentCorrelateCommand extends Command {
    static paths: string[][];
    static usage: import("clipanion").Usage;
    private cdProviderParam;
    private configurationRepo;
    private configurationShas;
    private dryRun;
    private fips;
    private fipsIgnoreError;
    private config;
    private logger;
    execute(): Promise<1 | undefined>;
    private sendCorrelationData;
    private validateTags;
    private handleError;
}

import { BaseCommand, RecursivePartial } from './base-command';
import { UploadApplicationCommandConfig } from './interfaces';
export declare class UploadApplicationCommand extends BaseCommand {
    static paths: string[][];
    static usage: import("clipanion").Usage;
    protected config: UploadApplicationCommandConfig;
    private mobileApplicationVersionFilePath;
    private mobileApplicationId;
    private versionName;
    private latest;
    static getDefaultConfig(): UploadApplicationCommandConfig;
    execute(): Promise<0 | 1>;
    protected resolveConfigFromEnv(): RecursivePartial<UploadApplicationCommandConfig>;
    protected resolveConfigFromCli(): RecursivePartial<UploadApplicationCommandConfig>;
}

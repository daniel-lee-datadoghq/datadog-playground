import { Command } from 'clipanion';
/**
 * This command is a wrapper around the datadog-ci tag command, allowing customers to mark CI jobs
 * as deployments and setting specific properties, like the environment or the revision in a simple way.
 */
export declare class DeploymentMarkCommand extends Command {
    static paths: string[][];
    static usage: import("clipanion").Usage;
    private noFail;
    private isRollback;
    private env;
    private revision;
    private service;
    private tags;
    private fips;
    private fipsIgnoreError;
    private config;
    execute(): Promise<number>;
    createJobDeploymentTags(): string[];
    createPipelineDeploymentTags(): string[];
}
